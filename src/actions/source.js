import axios from 'axios';
import {
  RECRUIT_LOADED,
  RECRUIT_ERROR,
  SOURCE_LOADED,
  SOURCE_ERROR
} from './types';

// Get current recruiting
export const getCurrentRecruit = () => async dispatch => {
  try {
  } catch (error) {}
};
